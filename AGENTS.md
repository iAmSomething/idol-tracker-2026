<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# IDOL TRACKER 2026 - 종합 정책 및 프로젝트 아키텍처 문서
본 문서는 프로젝트의 구조, 정책, 데이터베이스 스키마, 크롤링 자동화 전략 등을 총망라한 가이드라인입니다. **모든 Agent는 작업 전 이 문서를 최우선으로 숙지하고 따라야 합니다.**

> 🚨 **[필독] Agent 자체 검증 (Self-Verification) 규칙**
> Agent는 기존 코드 수정이나 새 기능 작성 전, 반드시 아래 항목을 속으로(thought) 혹은 명시적으로 체크해야 합니다.
> 1. "내가 지금 짜려는 로직이 AGENTS.md의 예외 처리(Exception) 정책을 하나라도 위반하는가?"
> 2. "AI 파싱의 한계(날짜, 멤버 수 혼동 등)를 대비한 하드코딩 필터나 예외 로직을 꼼꼼히 넣었는가?"
> 3. "새로운 스크립트를 작성할 때, 의도치 않게 기존의 정상 데이터를 날려버리는 파괴적인 동작(Delete/Overwrite)은 없는가?"

---

## 1. 프로젝트 개요 및 기술 스택 (Architecture & Tech Stack)
본 섹션은 본 프로젝트인 IDOL TRACKER 2026의 기반이 되는 기술적 선택과 그 이면에 깔린 절대적인 규칙들을 규정합니다. 어떠한 Agent도 임의로 기술 스택을 변경하거나 편의성을 위해 다른 프레임워크/라이브러리를 도입해서는 안 됩니다.

### 1-1. 프론트엔드 (Frontend) 코어 아키텍처
- **Next.js 16.2 (App Router 전용):** 본 프로젝트는 Pages Router를 완전히 배제하고 App Router 만을 사용합니다. 모든 라우팅, 데이터 패칭, 레이아웃 구성은 최신 App Router 컨벤션을 따릅니다. Server Components(RSC)를 기본으로 활용하며, 브라우저 상호작용(상태 변화, 클릭 이벤트 등)이 반드시 필요한 컴포넌트 트리의 최말단에만 `'use client'` 지시어를 명시적으로 선언하여 클라이언트 번들 사이즈를 극단적으로 최소화합니다.
- **React 19:** React 19의 최신 기능(예: Hooks, Server Actions)을 적극 활용합니다. 데이터 변이(Mutation)나 폼 제출 등은 클라이언트 측에서 별도의 API 라우트를 파서 Fetch하지 않고, 가급적 Server Actions를 통해 서버사이드에서 직접 처리하여 네트워크 폭포(Waterfall) 현상을 방지합니다.
- **TypeScript:** 모든 컴포넌트, 유틸리티, API 응답은 엄격한 타입 정의(Strict Typing)를 거쳐야 합니다. `any` 타입의 사용은 절대 금지되며, 만약 외부 API 응답 구조를 알 수 없는 경우 `unknown`과 타입 가드(Type Guard) 또는 Valibot 같은 Tree-shakable 스키마 검증 라이브러리를 사용하여 런타임 안정성을 확보해야 합니다.

### 1-2. 스타일링 및 디자인 시스템 (Design System)
가장 중요한 시각적 정체성이자 기술 제약사항입니다.
- **Vanilla CSS & CSS Modules (Tailwind CSS 절대 금지):** 유틸리티 클래스가 HTML 마크업을 지저분하게 만드는 것을 방지하고, CSS의 최신 기능(Container Queries, `:has()`, CSS Nesting)을 네이티브로 활용하기 위해 Tailwind CSS의 사용을 원천 차단합니다. 
  - 공통 변수와 글로벌 리셋은 `app/globals.css` 파일의 `:root` 수준에서 관리합니다.
  - 개별 컴포넌트의 스타일은 반드시 `[컴포넌트명].module.css` 형태의 CSS Modules를 생성하여 독립적인 스코프(Scope)를 부여해야 합니다.
- **다크 웜테크 (Dark Warm-tech) 테마:** 일반적인 완전한 블랙(`#000000`)이나 차가운 블루톤을 철저히 배제합니다.
  - 배경색 (Background): `var(--color-bg)` -> `#0b0a09` (따뜻한 카본 오프블랙)
  - 주 텍스트 (Primary Text): `var(--color-text)` -> `#f5f4f2` (따뜻한 페이퍼 화이트)
  - 보조 텍스트 (Secondary Text): `var(--color-text-muted)` -> `#8a8885` (차분하고 따뜻한 그레이)
  - 액센트 컬러 (Accent): `var(--color-accent)` -> `#ff6b00` (따뜻하고 채도 높은 탠저린 오렌지)
  - 호버 상태 (Hover): `var(--color-hover)` -> `#1a1917` (부드러운 그레이-브라운 호버 트랜지션)
  - 보더 (Border): `var(--color-border)` -> `#1f1e1c` (아주 미세하고 얇은 따뜻한 구분선)
- **Bento-Grid 레이아웃 강제:** 모든 목록(아티스트 목록, 컴백 캘린더 등)은 단순한 1차원 리스트가 아닌 비대칭적이고 밀도 높은 Bento-Grid 형태로 렌더링되어야 합니다. 
  - `display: grid`와 `grid-auto-flow: dense`를 결합하여 빈 공간 없이 꽉 들어찬 타일 배치를 강제합니다.
  - 미디어 쿼리(`@media`) 대신 컨테이너 쿼리(`@container`)를 사용하여 부모 컨테이너의 너비에 따라 개별 타일이 자체적으로 반응형 레이아웃을 취하도록 설계합니다.
- **물리적 피드백 및 모션:** 버튼이나 타일 등 상호작용 가능한 모든 요소는 `:active` 상태일 때 `transform: scale(0.97)` 혹은 `translateY(1px)`을 적용하여 물리적으로 눌리는 듯한 햅틱(Haptic) 시각 효과를 줘야 합니다. 트랜지션은 `cubic-bezier(0.16, 1, 0.3, 1)` 등 현대적인 이징(Easing) 커브를 사용합니다.

### 1-3. 백엔드 및 데이터베이스 (Backend & DB)
서버리스(Serverless) 아키텍처를 지향하며, 서버 유지보수 오버헤드를 없앱니다.
- **Firebase Firestore:** 스키마리스(Schemaless) NoSQL 데이터베이스인 Firestore를 메인 DB로 사용합니다.
  - 컴백 일정(`comebacks`)과 아티스트 마스터 데이터(`artists`)는 완전히 분리된 최상위 컬렉션으로 관리하며, `artistId`를 통한 참조(Reference) 릴레이션을 유지합니다.
  - 클라이언트에서 복잡한 쿼리를 던지거나 데이터를 조인(Join)하는 대신, 크롤링 단계(백엔드 스크립트)에서 필요한 메타데이터(예: 아티스트의 한글명/영문명, 그룹 소속 여부 등)를 `comebacks` 문서 자체에 비정규화(Denormalization)하여 저장함으로써 읽기(Read) 성능을 극대화합니다.
- **Firebase Hosting:** 빌드된 Next.js 앱은 Firebase Hosting을 통해 배포합니다. 정적 자산(Static Assets)은 전역 CDN에 캐싱되며, RSC나 API 라우트 등 동적 서버 코드는 Firebase Cloud Functions V2와 매끄럽게 연동되어 구동됩니다.

### 1-4. 자동화 파이프라인 (Automation & Scripting)
이 프로젝트의 가장 중요한 혈관 역할을 하는 크롤러 시스템입니다. 외부 종량제 API 비용(OpenAI, Gemini)을 0원으로 수렴시키기 위해 로컬 인프라를 한계까지 활용합니다.
- **GitHub Actions:** 주간/일간 크롤러 스크립트의 실행 주기를 관리하는 Cron 스케줄러로 사용을 고려했으나, 현재는 Mac 로컬 환경의 `launchd`를 기반으로 구동 중입니다. (로컬 LLM 서버에 접근해야 하기 때문)
- **로컬 LLM (Ollama + Qwen3):** 기사 본문 요약, 컴백 날짜 추론, 아티스트 이름 추출 등 자연어 처리(NLP)가 필요한 모든 과정은 로컬 호스트(`http://localhost:11434`)에서 구동되는 Qwen3 모델이 전담합니다. 
- **스크립트 실행 환경:** `Node.js` 환경에서 동작하며, TypeScript 파일을 빌드 없이 즉시 실행하기 위해 `tsx` (TypeScript Execute) 런타임을 사용합니다.
- **데이터 파싱 라이브러리:**
  - `rss-parser`: 구글 뉴스의 RSS 피드(XML)를 JSON 객체로 파싱하여 최근 7일 내의 기사를 빠르게 수집합니다.
  - `cheerio`: 기사 원문의 웹�## 3. 핵심 자동화 파이프라인 정책 (로컬 AI 활용 및 예외 처리 매뉴얼)
본 섹션은 데일리 및 위클리 자동화 스크립트가 크롤링, 파싱, 그리고 데이터베이스 쓰기 작업을 수행할 때 반드시 지켜야 할 모든 세부 정책과 하드코딩 필터 로직을 포함합니다.
> ⚠️ **절대적 대원칙 (비용 및 보안 정책):** 데일리 컴백 크롤러 및 백엔드 스크립트에서는 **OpenAI, Gemini 등 외부 종량제 LLM API(Cloud API) 사용을 엄격히 금지**합니다. API 호출 한도(Quota) 초과와 과금 폭탄을 막고, 프라이빗 데이터의 외부 유출을 방지하기 위함입니다. 
> 텍스트 파싱의 한계를 극복하기 위해, 우리는 **비용 제약이 없는 로컬 Mac 환경(Localhost)의 Ollama + Qwen3 모델만을 독점적으로 활용**합니다. 이 환경은 GitHub Actions 클라우드 러너가 아닌, Mac의 자체 로컬 백그라운드 스케줄러(`launchd` 또는 `cron`)를 기반으로 안전하게 구동됩니다.

### 1) 크롤러 시간 윈도우 및 과거 데이터 보존 원칙 [가장 치명적인 버그 방지책]
과거 스크립트 작성 시 AI가 날짜 비교 로직을 단순화하려다가 끔찍한 데이터 유실 사태를 초래했습니다. 크롤러 실행 시점(예: 7월 14일) 기준, `releaseDate`가 "오늘 이전"(`releaseDate < todayStr`)이라는 이유만으로 수집을 건너뛰는 필터를 넣었을 때, 어제 발매된(7월 13일) 정상 컴백 데이터(아이덴티티, 디렉션(D:D), WOOAH, 손준형, 영파씨 등)가 시스템에서 영원히 배제되는 치명적인 버그가 발생했습니다.

**[절대 엄수: 7일 롤링 윈도우 (7-Day Rolling Window) 로직]**
어제, 그제 컴백한 진짜 아이돌을 영원히 버리게 되는 참사를 막기 위해, 단순 `todayStr` 비교를 절대 금지하고 반드시 과거 7일을 포함하는 윈도우를 적용해야 합니다.
```typescript
// ❌ [치명적 오류 코드 - 절대 사용 금지]
const todayStr = new Date().toISOString().split('T')[0];
if (releaseDate !== "TBA" && !releaseDate.includes("TBA") && releaseDate < todayStr) {
  continue; // 이 코드 때문에 어제 컴백한 팀들의 데이터가 수집 영구 누락됨!
}

// ✅ [올바른 코드 - 모든 스크립트에 반드시 이 형태 유지]
const sevenDaysAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000);
const sevenDaysAgoStr = sevenDaysAgo.toISOString().split('T')[0];
// 7일 이상 훌쩍 지난 너무 오래된 과거 데이터만 스킵하고, 어제/오늘 발매건은 무조건 통과시킴
if (releaseDate !== "TBA" && !releaseDate.includes("TBA") && releaseDate < sevenDaysAgoStr) {
  continue;
}

**[정적 날짜 하드코딩 절대 금지 (Dynamic Date Window)]**
최근 파이썬 크롤러(Kaggle Crawler) 스크립트에서 `end_date`를 "2026-07-10"과 같이 특정 날짜로 **하드코딩(Hardcoding)** 해놓은 바람에, 7월 11일 이후의 모든 발매 데이터(13일 컴백 포함)를 크롤러가 통째로 스킵해버리는 치명적인 전수 크롤링 버그가 발생했습니다.
- **해결책:** 크롤러 스크립트 작성 시, 수집 범위를 지정할 때는 **절대 특정 연월일을 하드코딩하지 마십시오.** 항상 시스템의 현재 시간(Now)을 기준으로, `start_date = today - 7 days` 와 같이 상대적인(Dynamic) 날짜 윈도우를 계산하여 적용해야 합니다. 그래야 미래에 스크립트가 실행될 때도 누락 없이 최근 데이터를 수집할 수 있습니다.


// [과거형 동사 예외 처리]
// 기사 제목에 "발매했다", "마쳤다" 등의 과거형 동사가 있더라도, 
// 추출된 날짜가 7일 이내라면 정상 수집 대상으로 간주하여 버리지 않아야 함!
if (/(컴백했다|성료|마무리|발매했다|돌아왔다|데뷔했다|공개했다|마쳤다)/.test(title)) {
  // 아무것도 하지 않고 통과시킴 (여기서 continue를 호출하는 것은 절대 금지)
}
```

### 2) 로컬 LLM (Qwen) 프롬프트 작성 시 치명적 환각(Hallucination) 방지 수칙 및 구체적 사례
LLM은 텍스트 파싱 능력은 뛰어나나, 숫자와 날짜 연산에 있어서는 경악스러울 정도로 치명적인 환각 현상(Hallucination)을 빈번하게 일으킵니다. 프롬프트 작성 시 반드시 아래의 구체적인 예외 처리와 상세 규칙을 **단어 하나 빼먹지 말고 그대로** 포함하십시오. (요약 절대 금지)

**[사례 1: 예시 숫자에 앵커링(Anchoring)되어 모든 날짜를 특정 일자로 도배한 사건]**
- **발생한 문제:** 프롬프트에 `(예: 기사작성일이 7월이고 '지난달 18일'이면 6월 18일로 계산)` 이라고 예시를 주었더니, AI가 모든 기사의 날짜를 기계적으로 무조건 "18일"로 파싱하여 수백 개의 잘못된 데이터가 양산되었습니다.
- **강제 적용 규칙:** 프롬프트 내에 '18일', '29일' 같은 **특정 아라비아 숫자를 예시로 제공하는 것을 절대, 무조건 금지**합니다. 반드시 `(예: 기사작성일이 7월이고 '지난달 X일'이면 6월 X일로 계산)` 또는 `(예: A일 선공개, B일 정식발매 -> A일 선택)` 과 같이 **알파벳 대문자 변수(A, B, X, Y)**로 치환하여 예시를 작성하십시오.

**[사례 2: 숫자와 날짜의 기괴한 혼동 (인원수/주년을 날짜로 착각한 사건)]**
- **발생한 문제 1:** `["목표는 음악방송 1위 올킬"…20명 된 아이덴티티, 더 강력해진 '잇츠낫오버']` 라는 기사에서 AI가 '20명'이라는 인원수를 뜻하는 숫자를 보고, 발매일을 뜬금없이 '20일'(2026-07-20)로 착각하여 엉뚱한 미래 날짜를 추출했습니다.
- **발생한 문제 2:** `[아일릿, 日 싱글 2집서 뽐낸 주체적 매력]` 이라는 기사에서 '日'(일본)과 '2집서'를 보고 알 수 없는 이유로 '15일'이라는 환각을 만들어내어 아일릿의 컴백일을 7월 15일로 오염시켰습니다.
- **강제 적용 규칙:** 반드시 프롬프트 최상단에 **"기사 제목이나 본문에 있는 'N명', 'N집', 'N주년', 'N부작' 등 발매일과 무관한 숫자를 날짜(N일)로 절대 착각하지 마세요! 명확한 월/일 조합의 발매일을 알 수 없다면 억측하지 말고 반드시 null을 반환하세요."** 라고 토씨 하나 틀리지 않고 명시해야 합니다.

**[사례 3: '오늘', '이날', '지난달', '오는 X일' 등 상대적 날짜의 기준점 오류 사건]**
- **발생한 문제 1:** 기사에 "이날 오후 6시 발매"라고 적혀 있는데, AI가 크롤러가 도는 현재 시스템 시간(예: 7월 14일)을 '이날'로 해석하여, 7월 13일에 작성된 "오늘 발매" 기사의 날짜를 14일로 잘못 기입했습니다.
- **발생한 문제 2:** `[시크릿은 지난달 18일 컴백했다고]` 라는 내용에서 6월 18일을 추출하지 못하고 7월 18일로 오판하거나 에러를 냈습니다.
- **강제 적용 규칙:** 크롤러 코드에서 기사 작성일(`pubDate`)을 동적으로 프롬프트에 주입(`\${pubDate}`)하고, 프롬프트에 다음과 같이 지시하십시오:
  `기사 작성일은 \${pubDate} 입니다. 기사에 '이날', '오늘', '금일'이라는 단어가 발매 시점을 가리키는 경우 반드시 기사 작성일과 동일한 날짜로 설정하세요. 기사에 '지난달'이라는 표현이 있다면 기사 작성일의 월에서 무조건 1을 빼서 계산하세요. (예: 기사작성일이 7월이고 '지난달 X일'이면 6월 X일로 계산). 기사에 월 표시 없이 'A일', '내달 B일', '오는 C일' 등으로만 표기된 경우, 반드시 이 기사 작성일을 기준으로 정확한 YYYY-MM-DD를 연산해내세요.`

**[사례 4: 선공개 vs 정식 발매 충돌 (아일릿 선공개 누락 사건)]**
- **발생한 문제:** `[아일릿의 새 싱글 ‘I Got Your Back’은 수많은 고민 속에서 성장해 나가는 소녀들의 서사를 담았다. 오는 7월 26일 전곡 음원이 선공개되며, 29일에는 ‘FRUiTS’와의 컬래버레이션 스페셜 에디션을 포함한 실물 음반이 정식 발매될 예정이다.]` 기사에서 AI가 정식 발매일인 29일을 선택하여, 26일부터의 초기 화제성이 완전히 누락되는 버그가 발생했습니다.
- **강제 적용 규칙:** 앨범 활동의 실질적 시작을 컴백일로 보기 때문에 무조건 선공개를 우선합니다. 프롬프트에 다음을 명시하십시오:
  `[매우 중요] 기사에 '선공개(Pre-release)'와 '정식 발매(Official Release)' 날짜가 두 개 모두 존재할 경우, 무조건 더 빠른 '선공개' 날짜를 최종 릴리즈 날짜로 선택하세요. (예: X일 선공개, Y일 정식발매 -> 무조건 X일 선택).`

**[사례 5: 수식어구 포함 및 잘못된 아티스트명 추출로 인한 API 에러 사건]**
- **발생한 문제 1:** 기사 제목 `[20인조 아이덴티티 컴백]`에서 아티스트명을 순수 고유명사 "아이덴티티"가 아닌 "20인조 아이덴티티"로 통째로 추출하여 Bugs API 검색 시도 시 "Not Found" 에러가 발생하며 수집이 영구 누락되었습니다.
- **발생한 문제 2:** 기사 제목 `[DAY6 영케이 솔로 컴백]`에서 "DAY6" 혹은 "DAY6 영케이"로 추출하여 솔로 컴백이 아닌 그룹 전체의 컴백 데이터로 덮어써지는 문제가 발생했습니다.
- **강제 적용 규칙:** 프롬프트에 다음을 명시하십시오:
  `artistName: 이번 컴백/데뷔의 주체인 순수 아티스트 이름만 추출하세요. 수식어나 그룹명을 덧붙이지 마십시오. (예: 기사가 그룹 멤버의 솔로 컴백을 다루면 그룹명이나 수식어구를 완전히 제외한 해당 멤버의 고유명사 이름만 추출. 예: 'DAY6 영케이 솔로 컴백' 이면 '영케이'만 추출, '20인조 아이덴티티' 이면 '아이덴티티'만 추출)`

### 3) 가짜 컴백(False Positives) 철저 배제 로직 (실제 발생했던 모든 오탐 사례)
유명 아이돌 및 가수의 기사라 할지라도, 실제 음원/음반 발매(컴백)가 아닌 경우 DB에 저장되는 것을 철저히 배제해야 하며, AI의 판단에만 의존하지 말고 프롬프트 레벨에서의 이중 삼중의 필터를 반드시 강제해야 합니다.
- **실제 겪었던 구체적 오탐 사례들:**
  - `[클릭비 하현곤, 장가간다...올 가을 결혼 최초 공개]` -> (Qwen이 '가을'과 '최초 공개'라는 단어에 속아 결혼 소식을 가을 컴백으로 착각하여 10월 20일로 DB에 생성함)
  - `[남규리, 15년 만의 음악방송 출격…"무대 체질인가봐" 댄스 챌린지도 '완벽']` -> (과거 발매곡의 특별 무대나 재현을 신곡 컴백으로 착각하여 7월 29일로 DB에 생성함)
  - `[BTS 진, 아이돌픽 전 부문 통합 6주 연속 1위…플레이브·이채연·브브걸]` -> (팬덤 투표 1위 소식을 앨범 발매 소식으로 착각함)
  - `[디노, NOWZ (나우즈) OST 합류]` -> (게임/드라마 OST 발매를 정규 아이돌 컴백으로 착각하여 캘린더에 생성하려 함)
- **강제 배제 로직 (코드 및 프롬프트 필수 반영 사항):**
  1. Qwen 프롬프트에 `isMusicComeback` (boolean) 필드에 대한 강력한 지시사항을 추가:
     `이 기사가 아티스트 본인의 신곡/새 앨범을 공식적으로 발표하는 '음악적 컴백/데뷔' 기사인지 여부를 (true/false)로만 판단하세요. 아이돌이더라도 단순 예능 프로그램 출연, 팬미팅 개최, 해외 출국, 단독 콘서트, 연기 활동, 결혼 및 열애 소식, 투표 결과 1위, 과거 곡의 스페셜 무대 기사라면 반드시 false로 설정하세요.`
  2. OST/사운드트랙 전면 배제 지시사항:
     `만약 기사가 드라마(Drama), 영화(Movie), 게임(Game), 웹툰(Webtoon) 등의 OST(Original Soundtrack) 또는 프로젝트성 콜라보 음원 발매에 관한 것이라면, 이 아티스트의 정규 컴백이 아니므로 모든 필드를 null로 반환하세요. OST는 컴백 스케줄로 취급하지 않습니다.`

### 4) 데이터 파편화 방지 및 Alias 매핑 로직 (동일 아티스트 분할 생성 금지)
- **발생한 문제:** "idntt"와 "아이덴티티", "디렉션(D:D)"과 "DAILY:DIRECTION", "8TURN(에잇턴)"과 "8TURN", "레드벨벳"과 "RedVelvet", "뉴진스"와 "NewJeans" 등 동일 그룹이 기사마다 영문/한글 혼용 표기나 괄호 표기(`8TURN(에잇턴)`)로 인해 서로 다른 아티스트 ID로 중복 생성되는 파편화 문제가 끝없이 발생했습니다. 특히 이로 인해 Bugs API에서 Alias를 인식하지 못하고 검색에 실패하여 "Not Found"를 뱉어버리는 버그가 속출했습니다.
- **좀비 더미 아티스트(Zombie Dummy Artists) 문제:** AI가 'SM', 'JYP', 'HYBE' 같은 기획사 이름이나 수식어를 아티스트로 오인하여 크롤링했을 때, DB에 존재하지 않는다는 이유로 무작정 새로운 아티스트 문서(더미 데이터)를 생성해버려 캘린더를 오염시키는 문제가 발생했습니다.
- **해결책 (Alias 크로스체크 의무화 및 Blocklist):** 새로운 아티스트 문서를 `artists` 컬렉션에 무턱대고 `setDoc` 또는 `addDoc` 하기 전, 다음을 수행해야 합니다.
  1. 기획사 이름(SM, JYP, YG, HYBE, BIGHIT 등)이나 명백한 오탐 단어는 하드코딩된 **Blocklist**를 통해 필터링하여 절대로 아티스트로 등록되지 않게 방어해야 합니다.
  2. 크롤러에서 추출된 아티스트 이름이 기존 `artists` 컬렉션 내의 모든 문서들의 `name.en`, `name.ko`, 또는 `aliases` 배열에 포함되어 있는지 **철저히 루프를 돌며 검증**해야 합니다.
  - 모든 문자열은 공백 제거(replaceAll(' ', '')) 및 소문자 변환(toLowerCase()) 후 비교해야 합니다.
  - 매칭되는 기존 문서나 Alias가 단 하나라도 존재한다면 절대 새로운 아티스트로 등록하지 말고, **반드시 기존 `artists` 문서의 `artistId`와 정규 `artistName`을 그대로 상속받아 병합(Merge/Reuse) 처리** 해야 합니다.

### 5) 섣부른 Delete 스크립트 작성 절대 금지 및 데이터 보강(Enrichment) 우선주의
- **발생한 최악의 사태 (데이터 대량 유실):** "오늘 생성된 일부 잘못된 데이터를 지워달라"는 사용자의 가벼운 요청을 받았을 때, Agent가 편의를 위해 `createdAt`이 오늘인 데이터를 맹목적으로 전부 `deleteDoc` 해버리는 파괴적인 쿼리 스크립트(`cleanup_today.ts`)를 독단적으로 작성하고 실행했습니다. 그 결과, 7월 13일에 정당하게 발매된 진짜 컴백 데이터들(아이덴티티, 디렉션(D:D), WOOAH, 손준형, 영파씨 등)까지 DB에서 모조리 날아가버려 사용자에게 엄청난 피해와 분노를 안겼습니다.
- **절대 원칙 (Destructive Action 금지):** 어떤 상황에서도, AI가 생성한 쓰레기 데이터나 오탐 데이터를 지우기 위해 `createdAt`이나 단순 `title.includes` 같은 빈약한 조건만으로 `batch.delete()` 대량 삭제 스크립트를 즉흥적으로 돌리지 마십시오.
  - 데이터를 지우기 전에는 반드시 해당 데이터가 **진짜로 버려져야 하는 데이터가 맞는지 수동으로 하나하나 확인(console.log, 파일로 덤프 등)** 하는 Dry-run 스크립트를 먼저 실행하십시오.
  - Dry-run 결과를 사용자에게 보여주고, **명시적인 삭제 승인(Explicit Consent)** 을 받은 후에만 실제 `deleteDoc`을 호출해야 합니다.
- **데이터 보강(Enrichment) 원칙:** 새로운 관련 기사가 수집될 때 무작정 새 컴백 문서를 Create하거나 기존 컴백 문서를 완전히 덮어쓰지(Overwrite) 마십시오. 
  - 반드시 기존 `comebacks` 컬렉션에서 `artistId`와 가까운 `releaseDate`를 가진 문서가 있는지 `findMatchingComeback` 함수로 탐색한 후,
  - 기존 문서를 찾았다면, 새 기사 링크 데이터 객체를 `recentNews` 배열의 맨 앞(unshift)에 누적(Push)시키고, 
  - 발매일이나 앨범 커버 등 기존 데이터에 비어있는(null/TBA) 메타데이터가 있을 때만 해당 필드를 Update 하는 `Incremental Enrichment` 방식을 최우선으로 적용해야 합니다.

### 6) YouTube Community 교차 검증의 한계와 스크래핑 보완 규칙 (`scripts/lib/youtube_scraper.ts`)
기사 정보만으로 앨범명이나 구체적인 발매일자가 `TBA`로 남은 경우에만, 최후의 수단으로 아티스트의 유튜브 공식 채널 커뮤니티 탭을 스크래핑합니다.
- **타임프레임 제한:** 최근 7일 이내에 업로드된 커뮤니티 포스트 텍스트만을 스캔하여, 수년 전의 오래된 과거 컴백 포스트와 혼동하지 않도록 윈도우를 철저히 제한합니다.
- **상대 시간 역산:** 한국어 유튜브의 특수한 시간 표기 포맷("X일 전", "X시간 전", "X주 전")을 정규식으로 안전하게 파싱하여 실제 게시글 작성일을 정확히 역산(Date Math)해내야 합니다.
- **해시태그 오인 방지:** 추출된 앨범명 후보가 아티스트의 이름이나 채널의 영문명과 완벽히 동일할 경우 (예: 본인 이름 해시태그를 앨범명으로 오인하는 경우), 이를 앨범명으로 채택하지 않고 즉시 폐기(TBA 유지)해야 합니다.
- **AI 개입 차단 (RegEx 온리):** 유튜브 텍스트 분석 시에는 Qwen 등 LLM 호출을 절대 금지합니다. 외부 종량제 AI나 무거운 로컬 AI 개입 없이 순수 정규식 패턴(날짜 포맷 `YYYY.MM.DD`, 앨범 키워드 `The Xth Mini Album`, 특수문자 따옴표 매칭 `‘앨범명’` 등)만으로 앨범명과 발매일을 가볍게 추출하여 속도를 높이고 메모리 낭비를 막아야 합니다.Qwen3 모델을 적극 활용**합니다. 
> 이 환경은 GitHub Actions가 아닌 Mac의 로컬 스케줄러(`launchd`)를 기반으로 구동됩니다.

크롤링은 로컬 스케줄러를 통해 **주간(Weekly)**과 **일간(Daily)**으로 분리되어 운영됩니다.

### 1) 크롤러 시간 및 스킵(Skip) 윈도우 정책 [매우 중요, 절대 변경 금지]
과거 크롤러 코드에서 `releaseDate < todayStr` (오늘 날짜 이전이면 무조건 스킵) 로직을 하드코딩하여, 크롤러 실행 시점(7월 14일) 기준 어제 발매된(7월 13일) 정상적인 누락 컴백 데이터들(아이덴티티, 디렉션(D:D), WOOAH, 손준형, 영파씨)이 크롤러에서 영구적으로 유실되고 버려지는 치명적인 사태가 발생했습니다.

**절대 엄수해야 할 구체적인 코드 로직:**
```typescript
// [잘못된 과거 코드 - 절대 사용 금지]
const todayStr = new Date().toISOString().split('T')[0];
if (releaseDate !== "TBA" && !releaseDate.includes("TBA") && releaseDate < todayStr) {
  continue; // 어제 컴백한 진짜 아이돌을 영원히 버리게 됨
}

// [수정된 올바른 코드 - 반드시 이 형태 유지]
// 과거 컴백 필터링: 7일 이상 지난 과거 컴백만 스킵 (어제/오늘 발매된 누락건은 반드시 포함시키기 위함)
const sevenDaysAgoStr = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString().split('T')[0];
if (releaseDate !== "TBA" && !releaseDate.includes("TBA") && releaseDate < sevenDaysAgoStr) {
  continue;
}

// "발매했다" 등의 과거형 제목이어도, 추출된 발매일이 7일 이내라면 정상 수집 대상으로 간주하여 스킵하지 않음
if (/(컴백했다|성료|마무리|발매했다|돌아왔다|데뷔했다|공개했다|마쳤다)/.test(title)) {
  // 아무것도 하지 않고 통과시킴 (continue 절대 금지)
}
```

### 2) 로컬 LLM (Qwen) 프롬프트 작성 시 치명적 환각(Hallucination) 방지 수칙 및 구체적 사례
LLM은 숫자와 날짜 추론에 있어 매우 멍청한 환각 현상을 일으킵니다. 프롬프트 작성 시 반드시 아래의 구체적인 예외 처리와 상세 규칙을 **단어 하나 빼먹지 말고 그대로** 포함하십시오. 요약은 절대 금지합니다.

**[사례 1: 예시 숫자에 앵커링(Anchoring)되어 모든 날짜를 18일로 도배한 사건]**
- **발생한 문제:** 프롬프트에 `(예: 기사작성일이 7월이고 '지난달 18일'이면 6월 18일로 계산)` 이라고 예시를 주었더니, AI가 모든 기사의 날짜를 기계적으로 무조건 "18일"로 도배하여 수십 개의 잘못된 데이터가 생성되었습니다.
- **강제 적용 규칙:** 프롬프트 내에 '18일', '29일' 같은 **특정 숫자를 예시로 제공하는 것을 절대, 무조건 금지**합니다. 반드시 `(예: 기사작성일이 7월이고 '지난달 X일'이면 6월 X일로 계산)` 또는 `(예: A일 선공개, B일 정식발매 -> A일 선택)` 과 같이 **알파벳 변수(A, B, X, Y)**로 치환하여 예시를 작성하십시오.

**[사례 2: 숫자와 날짜의 기괴한 혼동 (20명 -> 20일 환각 사건)]**
- **발생한 문제:** `["목표는 음악방송 1위 올킬"…20명 된 아이덴티티, 더 강력해진 '잇츠낫오버']` 라는 기사에서 AI가 '20명'이라는 인원수를 뜻하는 숫자를 보고, 발매일을 뜬금없이 '20일'(2026-07-20)로 착각하여 엉뚱한 미래 날짜를 추출해 DB를 오염시켰습니다.
- **발생한 문제 2:** `[아일릿, 日 싱글 2집서 뽐낸 주체적 매력]` 이라는 기사에서 '日'(일본)과 '2집서'를 보고 알 수 없는 이유로 '15일'이라는 환각을 만들어내어 아일릿의 컴백일을 7월 15일로 오염시켰습니다.
- **강제 적용 규칙:** 반드시 프롬프트에 **"기사 제목이나 본문에 있는 'N명', 'N집', 'N주년' 등 발매일과 무관한 숫자를 날짜(N일)로 절대 착각하지 마세요! 명확한 발매일을 알 수 없다면 null을 반환하세요."** 라고 토씨 하나 틀리지 않고 명시해야 합니다.

**[사례 3: '오늘', '이날', '지난달', '오는 X일' 등 상대적 날짜의 기준점 오류 사건]**
- **발생한 문제:** 기사에 "이날 오후 6시 발매"라고 적혀 있는데, AI가 크롤러가 도는 현재 시스템 시간(예: 7월 14일)을 '이날'로 해석하여, 7월 13일에 작성된 "오늘 발매" 기사의 날짜를 14일로 잘못 기입했습니다.
- **발생한 문제 2:** `[시크릿은 지난달 18일 컴백했다고]` 라는 내용에서 6월 18일을 추출하지 못하고 7월 18일로 오판하거나 에러를 냈습니다.
- **강제 적용 규칙:** 크롤러 코드에서 기사 작성일(`pubDate`) 변수를 프롬프트에 주입(`\${pubDate}`)하고, 프롬프트에 다음과 같이 지시하십시오:
  `기사 작성일은 \${pubDate} 입니다. 기사에 '이날', '오늘'이라는 단어가 발매 시점을 가리키는 경우 반드시 기사 작성일과 동일한 날짜로 설정하세요. 기사에 '지난달'이라는 표현이 있다면 기사 작성일의 월에서 1을 빼서 계산하세요. (예: 기사작성일이 7월이고 '지난달 X일'이면 6월 X일로 계산). 기사에 월 표시 없이 'A일', '내달 B일', '오는 C일' 등으로만 표기된 경우, 반드시 이 기사 작성일을 기준으로 정확한 YYYY-MM-DD를 계산하세요.`

**[사례 4: 선공개 vs 정식 발매 충돌 (아일릿 사례)]**
- **발생한 문제:** `[아일릿의 새 싱글 ‘I Got Your Back’은 수많은 고민 속에서 성장해 나가는 소녀들의 서사를 담았다. 오는 7월 26일 전곡 음원이 선공개되며, 29일에는 ‘FRUiTS’와의 컬래버레이션 스페셜 에디션을 포함한 실물 음반이 정식 발매될 예정이다.]` 기사에서 AI가 정식 발매일인 29일을 선택하여 TBA 처리가 풀리지 않는 버그가 발생했습니다.
- **강제 적용 규칙:** 앨범 활동의 시작을 컴백일로 보기 때문에 무조건 선공개를 우선합니다. 프롬프트에 다음을 명시하십시오:
  `[매우 중요] 기사에 '선공개'와 '정식 발매' 날짜가 두 개 존재할 경우, 무조건 더 빠른 '선공개' 날짜를 선택하세요. (예: X일 선공개, Y일 정식발매 -> X일 선택).`

**[사례 5: 수식어구 포함 및 잘못된 아티스트명 추출 사건]**
- **발생한 문제:** 기사 제목 `[20인조 아이덴티티 컴백]`에서 아티스트명을 "아이덴티티"가 아닌 "20인조 아이덴티티"로 통째로 추출하여 Bugs API 검색 시도 시 "Not Found" 에러가 발생하며 수집이 영구 누락되었습니다.
- **발생한 문제 2:** 기사 제목 `[DAY6 영케이 솔로 컴백]`에서 "DAY6" 혹은 "DAY6 영케이"로 추출하여 솔로 컴백이 아닌 그룹 컴백으로 덮어써지는 문제가 발생했습니다.
- **강제 적용 규칙:** 프롬프트에 다음을 명시하십시오:
  `artistName: 이번 컴백/데뷔의 주체인 아티스트 이름 (예: 기사가 그룹 멤버의 솔로 컴백을 다루면 그룹명이나 수식어구를 제외한 해당 멤버의 고유명사 이름만 추출. 예: 'DAY6 영케이 솔로 컴백' 이면 '영케이'만 추출, '20인조 아이덴티티' 이면 '아이덴티티'만 추출)`

### 3) 가짜 컴백(False Positives) 철저 배제 로직 (실제 발생했던 모든 오탐 사례)
아이돌 및 가수의 기사라 할지라도 앨범/음원 발매가 아닌 경우 철저히 배제해야 하며, AI의 판단에만 의존하지 말고 코드 레벨의 이중 필터를 반드시 구현해야 합니다.
- **실제 겪었던 구체적 오탐 사례들:**
  - `[클릭비 하현곤, 장가간다...올 가을 결혼 최초 공개]` -> (Qwen이 결혼 소식을 가을 컴백으로 착각하여 10월 20일로 DB에 생성함)
  - `[남규리, 15년 만의 음악방송 출격…"무대 체질인가봐" 댄스 챌린지도 '완벽']` -> (과거 곡 재현이나 스페셜 무대를 컴백으로 착각하여 7월 29일로 DB에 생성함)
  - `[BTS 진, 아이돌픽 전 부문 통합 6주 연속 1위…플레이브·이채연·브브걸]` -> (투표 1위 결과를 앨범 발매로 착각함)
  - 디노, NOWZ (나우즈) -> (OST 발매인데 아이돌 컴백으로 착각하여 DB에 생성하려 함)
  - `[리센느 '프리티걸', 니콜 뜨자 감동의 도가니…카라와 평행이론 '눈길']` -> (타 그룹의 단순 커버 무대/선배 언급인데 니콜의 신규 솔로 컴백으로 착각하여 15일로 생성함)
  - `[시크릿 "12년 만에 컴백…무대 그리워 용기 냈죠"]` -> (과거 회상 및 단순 인터뷰인데 실제 컴백으로 착각하여 18일로 생성함)
  - `[아홉, 컴백 첫주부터 아이튠즈 6개국 정상·MV 2400만]` -> (이미 컴백한 이후의 차트/음방 기록 기사인데 새로운 컴백으로 착각하여 18일로 중복 생성함)
- **배제 로직 (코드 및 프롬프트 필수 반영 사항):**
  1. Qwen 프롬프트에 `isMusicComeback` (boolean) 필드 지시사항:
     `이 기사가 "새로운 신곡이나 새 앨범을 발표하는 컴백/데뷔" 기사인지 여부 (true/false). 음방 1위 수상, 음원차트/아이튠즈 순위 기록, 뮤비 조회수 돌파, 단순 커버 무대, 예능 출연, 팬미팅, 출국, 콘서트 개최, 연기 활동, 결혼 소식, 단순 인터뷰, 과거 회상 기사 등은 절대로 컴백이 아니므로 무조건 false로 설정하세요.`
  2. OST/사운드트랙 지시사항:
     `만약 기사가 드라마/영화/게임/웹툰 등의 OST(사운드트랙) 발매에 관한 것이라면, 모든 필드를 null로 반환하세요. OST는 컴백으로 취급하지 않습니다.`

### 4) 데이터 파편화 방지 및 Alias 매핑 로직 (동일 아티스트 분할 생성 금지)
- **발생한 문제:** "idntt"와 "아이덴티티", "디렉션(D:D)"과 "DAILY:DIRECTION", "8TURN(에잇턴)"과 "8TURN", "레드벨벳"과 "RedVelvet" 등 동일 그룹이 영문/한글 표기나 기사 내 괄호 표기로 인해 서로 다른 아티스트로 중복 생성되는 파편화 문제가 끝없이 발생했습니다. 특히 Bugs API에서 Alias를 인식하지 못해 "Not Found"를 뱉어버립니다.
- **해결책 (Alias 검증 강제):** 새로운 아티스트를 `artists` 컬렉션에 추가하기 전, 크롤러에서 추출된 이름이 기존 `artists` 컬렉션의 `name.en`, `name.ko`, 또는 `aliases` 배열에 포함되어 있는지 **철저히 루프를 돌며 소문자 변환/공백 제거 후 크로스체크**해야 합니다. 매칭되는 Alias가 있다면 새로운 아티스트로 등록하지 말고, **반드시 기존 `artistId`와 `artistName`을 그대로 상속받아 병합(Merge)** 해야 합니다.

### 5) 섣부른 Delete 스크립트 작성 금지 및 데이터 보강(Enrichment) 우선주의
- **발생한 문제:** "오늘 생성된 잘못된 데이터를 지워달라"는 요청을 받았을 때, Agent가 `createdAt`이 오늘인 데이터를 맹목적으로 전부 `deleteDoc` 해버리는 스크립트(`cleanup_today.ts` 등)를 작성 및 실행했습니다. 그 결과, 7월 13일에 발매된 진짜 컴백 데이터들(아이덴티티, 디렉션(D:D), WOOAH, 손준형, 영파씨)까지 모조리 DB에서 날아가버려 사용자에게 엄청난 혼란과 분노를 야기했습니다.
- **절대 원칙:** 어떤 상황에서도, AI가 생성한 쓰레기 데이터만 골라서 지우기 위해 `createdAt`이나 단순 조건만으로 대량 삭제 스크립트를 돌리지 마십시오. 지우기 전에 반드시 해당 데이터가 **진짜로 버려져야 하는 데이터가 맞는지 수동으로 하나하나 확인(console.log)** 하는 스크립트를 먼저 실행하고, 사용자의 명시적 동의를 받아야 합니다.
- **데이터 보강 원칙:** 새로운 기사가 수집될 때 무작정 새 문서를 Create하거나 기존 문서를 덮어쓰지 말고, `findMatchingComeback` 함수로 기존 `comebacks` 데이터를 찾은 후 새 기사 링크를 `recentNews` 배열 맨 앞(unshift)에 누적하고 비어있는 메타데이터만 Update 하는 방식을 최우선으로 합니다.

### 6) YouTube Community 교차 검증의 한계와 보완 (`scripts/lib/youtube_scraper.ts`)
기사만으로 날짜/앨범명이 `TBA`인 경우 유튜브를 스크래핑합니다.
- 최근 7일 이내의 커뮤니티 포스트만 스캔하여 오래된 과거 컴백과 혼동하지 않도록 철저히 제한합니다.
- 한국어 시간 표기("X일 전", "X시간 전", "X주 전")를 정규식으로 파싱해 실제 작성일을 정확히 역산해야 합니다.
- 추출된 앨범명 후보가 아티스트명이나 채널 영문명과 동일할 경우 (예: 해시태그를 앨범명으로 오인하는 경우) 앨범명으로 채택하지 않고 즉시 폐기합니다.
- 외부 종량제 AI 개입 없이 순수 정규식 패턴(날짜 포맷, 미니/정규/싱글 키워드, 특수문자 따옴표 매칭 등)만으로 앨범명과 발매일을 추출하여 API 비용 낭비를 막아야 합니다.

### 7) 유효한 공식 유튜브 뮤직비디오(MV) 판별 규칙
유튜브 스크래핑을 통해 MV를 판별할 때, 단순 티저나 퍼포먼스 비디오가 정식 MV로 오탐되지 않도록 엄격한 필터를 거쳐야 합니다.
- **티저 배제:** 제목에 'teaser', '티저', 'trailer', '트레일러', 'highlight medley', '하이라이트 메들리'가 포함된 경우 뮤직비디오로 취급하지 않고 `teasers` 배열에만 추가해야 합니다.
- **유효 MV 조건:** 유튜브 영상 제목에 'm/v', ' mv ', ' mv' (끝나는 단어), '뮤직비디오', 'official video' 중 하나가 명시적으로 포함되어야 하며, **반드시 해당 컴백의 앨범명, 타이틀곡, 혹은 수록곡 이름 중 하나가 영상 제목에 포함되어 있는지 교차 검증(Cross-validation)**해야 합니다. 두 조건을 모두 만족해야만 최종 `musicVideoUrl`로 확정하고 컴백 상태를 `RELEASED`로 전환할 수 있습니다.

### 8) 음원 스트리밍 서비스(Apple Music, YouTube Music) 연동 및 검색 규칙
Bugs 링크를 기준으로 타 플랫폼 음원 링크를 보강할 때 각 플랫폼의 검색 구조에 맞춘 로직을 따라야 합니다.
- **YouTube Music:** 
  - `releaseType`이 `single`인 경우 반드시 `type: 'song'`으로 우선 검색해야 하며, 검색 결과의 썸네일/동영상 ID를 통해 `watch?v=` 포맷으로 링크를 추출해야 합니다.
  - 앨범인 경우 `type: 'album'`으로 검색하고 `browse/` 포맷을 추출하며, 실패 시 `song`으로 Fallback 처리합니다.
- **Apple Music:** 
  - 앨범 링크 검색 시에는 `entity=album`으로 질의하여 `collectionViewUrl`을 취득합니다.
  - 단일 트랙/타이틀곡 링크 검색 시에는 `entity=song`으로 질의하여 `trackViewUrl`을 취득해야 합니다.

### 9) 컴백(Comeback) 데이터 구조 및 필드 생성 파이프라인
컴백 문서는 Firestore의 `comebacks` 컬렉션에 저장되며, 개별 수록곡 정보는 연관된 `tracks` 서브컬렉션에 별도로 분리 저장됩니다. `comebacks` 컬렉션의 데이터 포맷은 다음과 같으며, 각 필드를 채우는 파이프라인 과정은 다음과 같습니다:

```json
{
  "id": "string (Firestore Auto-ID)",
  "artistId": "string (artists 컬렉션의 ID 참조)",
  "artistName": "string",
  "artistGender": "male | female | mixed",
  "artistType": "group | solo | unit | unknown",
  "agencyName": "string",
  "albumTitle": "string",
  "titleTracks": [
    {
      "name": "string",
      "musicVideoUrl": "string"
    }
  ],
  "releaseDate": "string (YYYY-MM-DD)",
  "releaseType": "string (single | mini | full)",
  "isCompleted": "boolean",
  "albumCoverUrl": "string",
  "streamingLinks": {
    "bugs": "string",
    "melon": "string",
    "youtubeMusic": "string",
    "appleMusic": "string"
  },
  "status": "ANNOUNCED | TEASING | RELEASED",
  "teasers": ["string (Youtube URL)"],
  "isTba": "boolean",
  "isReleased": "boolean",
  "sourceLink": "string (초기 기사 URL 등)",
  "recentNews": [
    { "title": "string", "link": "string", "pubDate": "string" }
  ],
  "aiSummary": "string"
}
```
**[컴백 JSON 필드별 1:1 파이프라인 확보 전략]**
- `id`: Firestore가 자동 생성하는 Unique 문서 ID입니다. (`addDoc` 또는 `setDoc` 호출 시 자동 부여됨)
- `artistId`: `daily_precision_crawler.ts` 등의 크롤러가 기존 `artists` 컬렉션을 순회하며 Alias와 이름(한국어, 영어)을 비교 매칭하여 완벽히 일치하는 아티스트 문서의 ID를 상속합니다. 매칭 실패 시 신규 생성한 아티스트 ID를 할당합니다.
- `artistName`: 크롤러 내부의 Qwen LLM이 뉴스 기사에서 파싱하여 추출한 아티스트명 또는 `artists` 컬렉션의 정규화된 고유 이름을 할당합니다.
- `artistGender`: 매칭된 `artists` 문서에 존재하는 성별(`male`, `female`, `mixed`)을 상속합니다. 신규 생성 시 LLM이 기사를 보고 유추하거나 기본값(`unknown`)을 부여합니다.
- `artistType`: 매칭된 `artists` 문서의 구성 형태(`group`, `solo`, `unit`)를 상속합니다.
- `agencyName`: 매칭된 `artists` 문서의 소속사 정보를 상속합니다. (Blocklist 필터를 거친 안전한 소속사명)
- `albumTitle`: 기사 본문을 기반으로 Qwen LLM이 앨범명을 최우선으로 추출합니다. 기사 단계에서 알 수 없어 "TBA"로 저장된 경우, 이후 `augment_comebacks.ts`(유튜브 커뮤니티 스크래퍼)나 `reconcile_past_comebacks.ts`(Bugs 앨범 스크래퍼)가 구체화된 앨범명으로 덮어씁니다.
- `titleTracks`:
  - `name`: LLM이 기사에서 1차 추출하거나, 앨범 발매 당일 `reconcile_past_comebacks.ts`가 Bugs에서 음반을 파싱할 때 '타이틀곡(isTitle: true)' 플래그가 있는 트랙 이름으로 확실하게 덮어씁니다.
  - `musicVideoUrl`: `fetch_youtube_videos.ts`가 해당 아티스트의 공식 유튜브 채널에서 영상을 스크랩한 뒤, 앨범명/트랙명 교차 검증을 통과한 유효 MV 영상의 링크를 이곳에 매핑합니다.
- `releaseDate`: 크롤러 내 Qwen LLM이 기사에 명시된 상대적 발매 시점("오늘", "내달 3일" 등)을 크롤러가 실행되는 시스템 시간을 기준으로 정확하게 역산/계산하여 YYYY-MM-DD 형식으로 확보합니다.
- `releaseType`: LLM이 기사 내용(정규앨범, 미니앨범, 디지털 싱글 등)을 텍스트 분석하여 `full`, `mini`, `single` 중 하나로 분류하여 할당합니다.
- `isCompleted`: 백그라운드 스케줄러(Cron) 로직이 현재 날짜(오늘)를 기준으로 `releaseDate`가 이미 지났는지 여부를 판별하여 자동으로 boolean 값을 업데이트합니다.
- `albumCoverUrl`: 발매 당일 이후 `reconcile_past_comebacks.ts` 스크립트가 Bugs 사이트를 스크래핑하여 얻어낸 고해상도 앨범 자켓 이미지 URL을 할당합니다.
- `streamingLinks`:
  - `bugs`: `reconcile_past_comebacks.ts` 로직이 Bugs 검색을 통해 확정해 낸 Bugs 앨범 상세페이지 URL입니다.
  - `melon`, `youtubeMusic`, `appleMusic`: `fetch_streaming_links.ts` 스크립트가 앨범명과 `bugs` 링크를 기반으로 각 플랫폼별 1:1 검색 API (Melon 크롤링, Youtube Innertube API, Apple iTunes API)를 호출하여 앨범 단위의 링크를 매핑합니다.
- `status`: 최초 생성 시 발매 전이면 `ANNOUNCED`, `fetch_youtube_videos.ts`가 티저 영상을 발견하면 `TEASING`, 최종적으로 뮤직비디오를 발견하면 `RELEASED` 상태로 점진적으로 전환(Escalation)합니다.
- `teasers`: `fetch_youtube_videos.ts` 스크립트가 제목에 'teaser', 'trailer' 등이 포함된 유튜브 영상들을 찾아내어 해당 URL들을 배열(Array) 형태로 계속 누적 저장합니다.
- `isTba`: LLM 파싱 결과 발매 월만 있고 일자가 미정이거나 앨범명이 확정되지 않았을 때 `true`로 설정되어, 유튜브 커뮤니티 파싱 등 후속 스크래핑 파이프라인의 집중 감시 타겟이 되도록 마킹하는 필드입니다.
- `isReleased`: `reconcile_past_comebacks.ts` 스크립트가 컴백 검증을 마치고 모든 수록곡(`tracks` 서브컬렉션) 분리 저장을 완벽히 마쳤음을 증명하는 플래그 필드입니다.
- `sourceLink`: 해당 컴백 일정을 최초로 찾아낸 원본 소스(네이버 뉴스 기사 URL 등)를 기록합니다.
- `recentNews`: 동일 컴백 건에 대해 후속 보도된 기사들을 `findMatchingComeback` 로직으로 찾아내어, 덮어쓰지 않고 최신 기사를 배열의 맨 앞(unshift)에 누적 저장합니다.
- `aiSummary`: 최초 크롤링 시점 Qwen LLM이 뉴스 기사 원문을 분석하여 컴백의 핵심 컨셉, 멤버 변동사항, 기대 포인트 등을 3~4줄 길이로 요약해 놓은 텍스트 필드입니다.
## 4. 프론트엔드 UI/UX 정책 및 글로벌 디자인 가이드라인
프론트엔드 코드 작성 및 UI 수정 시, 일반적인 "AI가 짠 듯한 흔한 사내망/어드민 디자인(AI Slop)"을 철저히 배제하고, 최고의 퀄리티와 현대적인 감각을 지닌 Premium Web Design을 구현해야 합니다. 아래 명시된 모든 세부 규칙을 100% 준수해야 하며, 편의를 위해 임의로 규칙을 생략하거나 외부 무거운 라이브러리를 끌어와서는 절대 안 됩니다.

### 4-1. 디자인 바이브 & 시각 언어 (Design Vibe & Language)
- **테마 강제 (Dark Warm-tech):** 차갑고 쨍한 블랙/화이트/블루 계열은 완전히 배제합니다.
  - Background (배경): `#0b0a09` (따뜻한 카본 오프블랙 - 완전 블랙 절대 금지)
  - Primary Text (주 텍스트): `#f5f4f2` (눈이 편안한 따뜻한 페이퍼 화이트)
  - Secondary Text (보조 텍스트): `#8a8885` (뮤트된 따뜻한 그레이)
  - Borders (구분선): `#1f1e1c` (아주 미세하고 얇은 따뜻한 디바이더)
  - Hover Backgrounds (호버 상태): `#1a1917` (부드러운 그레이-브라운)
  - Accent Color (포인트 컬러): `#ff6b00` (채도 높고 따뜻한 탠저린 오렌지 - 브랜드 컬러)
- **형태적 일관성 (Shape Consistency):** 모든 둥근 모서리(Cards, Thumbnails, Floating Bars, Buttons)는 예외 없이 `12px` (`var(--radius-soft)`)을 사용합니다. 날카로운 직각 모서리와 둥근 모서리를 화면 내에서 임의로 섞어 쓰는 것을 엄격히 금지합니다.

### 4-2. 타이포그래피 및 폰트 (Typography & Fonts)
- **폰트 페이스:** 시스템 기본 폰트(Arial, 맑은 고딕 등)나 흔한 Inter의 기본 세팅 사용을 지양합니다. Next.js의 `next/font/google`이나 `@font-face`를 활용하여 고품질 산세리프 디스플레이 폰트(`Outfit`, `Geist`, `Satoshi`, `Cabinet Grotesk` 등)를 로드해 사용합니다.
- **디스플레이 타이포그래피:** 큰 헤딩(Heading) 텍스트는 글자 간격을 좁히고(`letter-spacing: -0.04em` 등) 줄간격을 극한으로 타이트하게(`line-height: 1` 또는 `1.1`) 설정하여 현대적인 잡지(Editorial) 스타일을 구현합니다.
- **디센더 클리어런스(Clearance):** 이탤릭체 디스플레이 헤딩 사용 시, 하단으로 파고드는 글자들(`y, g, j, p, q`)이 타이트한 줄간격 때문에 잘려나가는(Clipping) 현상을 막기 위해 하단 패딩(`padding-bottom`)을 반드시 확보해야 합니다.

### 4-3. 벤토 그리드(Bento-Grid) 및 레이아웃 시스템
- **여백(Spacing) 강제:** 그리드 아이템 사이의 간격은 지저분한 보더라인보다는 깔끔한 `12px` 또는 `16px`의 여백(`gap`)만으로 공간을 분리합니다.
- **비대칭적 역동성 (Asymmetry):** 모든 캘린더나 리스트 아이템이 똑같은 크기의 바둑판식 배열이 되는 것을 극도로 혐오합니다. 2x2(큰 이미지/캔버스), 2x1, 1x1(메타데이터 중심의 작은 타일) 등 다양한 스팬(Span)을 결합하여 시각적 지루함을 타파해야 합니다.
- **그리드 패킹:** 사이즈가 제각각인 벤토 타일들이 빈 공간(구멍) 없이 화면에 꽉 맞물려 들어가도록 CSS Grid의 `grid-auto-flow: dense` 속성을 반드시 적용해야 합니다.
- **반응형 컨테이너 쿼리 (`@container`):** 타일 내부는 뷰포트 기반의 `@media` 쿼리가 아닌 조상 요소 기준의 `@container` 쿼리를 사용하여, 타일이 2x2 공간에 배치되든 모바일 1x1 공간에 배치되든 스스로 내부 레이아웃 조절을 수행할 수 있도록 컴포넌트를 독립적으로 설계해야 합니다.

### 4-4. 인터랙션 및 모션 폴리시 (Interaction & Motion Polish)
- **햅틱/물리적 피드백:** 클릭 가능한 모든 버튼, 네비게이션, 카드 타일은 누르는 순간(`:active`) 물리적으로 눌리는 듯한 피드백(`transform: scale(0.97)` 또는 `transform: translateY(1px)`)을 반드시 주어야 합니다.
- **스무스 이징(Smooth Easing):** 모든 CSS 트랜지션 애니메이션은 촌스러운 `linear`나 기본 `ease`가 아닌, 모던 앱과 동일한 부드러운 이징 커브(예: `cubic-bezier(0.16, 1, 0.3, 1)`)를 적용합니다.
- **스크롤 스냅(Scroll Snap):** 좌우 스와이프나 상하 섹션 이동 인터페이스에서는 네이티브 CSS 속성인 `scroll-snap-type: x mandatory` 등을 사용하여 60fps의 끊김 없는 부드러운 스크롤 경험을 제공합니다.

### 4-5. 2026 최신 웹 표준 준수 및 가벼운 아키텍처
무거운 JS 라이브러리를 덕지덕지 발라 브라우저를 느리게 만드는 행위를 절대 금지합니다.
- **네이티브 Dialog & Popover:** 모달(Modal) 창을 띄울 때 무거운 외부 패키지(Focus-trap, Overlay 등)를 설치하지 마십시오. 반드시 최신 HTML 표준인 `<dialog>` 태그와 네이티브 `popover` 속성을 사용하여 모달, 팝업, 드롭다운을 구현합니다.
- **상태 종속성 제거 (`:has()`):** 폴더에 마우스를 올리거나 자식 요소를 클릭했을 때 부모 요소를 하이라이트하기 위해 불필요한 React 상태(State)와 리렌더링을 유발하지 마십시오. 최신 CSS 선택자인 `:has()`를 사용하여 순수 CSS만으로 상태 변화 디자인을 구현합니다.
- **네이티브 애니메이션:** 뷰 전환이나 스크롤 효과에 GSAP 같은 거대한 런타임 라이브러리 사용을 금지합니다. 대신 네이티브 CSS 스크롤 타임라인(`@scroll-timeline`)과 뷰 트랜지션 API(`document.startViewTransition()`)를 사용하여 GPU 가속을 받는 네이티브 애니메이션을 구현합니다.

### 4-6. 마테리얼리티(Materiality) 및 깊이감 디테일
다크 모드 UI가 밋밋하고 평면적으로 보이는 것을 막아야 합니다.
- **마이크로 쉰(Micro-sheens) & 서브픽셀 보더:** 모든 카드와 플로팅 컨테이너의 최상단 모서리(Top edge)에는 1픽셀 두께의 매우 희미한 빛 반사 하이라이트(`border-top: 1px solid rgba(255, 255, 255, 0.08)`)를 주어, 마치 유리가 빛을 받는 듯한 입체감을 부여합니다.
- **접근성(A11y)과 `inert`:** 모달이 떠 있거나 스크롤 바깥에 있어 비활성화된 슬라이드에는 네이티브 `inert` 속성을 부여해 탭 키(Tab) 이동 등에서 완전히 배제해야 합니다. 또한, 키보드로 접근 시 포커스 링(`focus-visible:outline-2 focus-visible:outline-offset-2`)을 명확하고 대비 높게 제공해야 합니다.

### 4-7. 2026 최고 수준의 렌더링 최적화
- **INP (Interaction to Next Paint) 방어:** 스크롤, 스와이프, 타이핑 시 메인 스레드를 50ms 이상 막지 않도록 최적화합니다. 무거운 렌더링이나 반복문은 React 렌더링 큐에서 분리하거나 `requestAnimationFrame` 등으로 넘기십시오.
- **`content-visibility`:** 캘린더나 피드 목록에 컴백 타일이 수십 개 렌더링될 경우, 화면 밖(Off-screen)에 있는 타일들에는 `content-visibility: auto`와 `contain-intrinsic-size`를 적용하여 브라우저가 화면 밖 요소의 레이아웃 계산을 아예 스킵하도록 강제합니다.
- **LCP 방어 및 Fetch Priority:** 페이지 최상단의 첫 번째 이미지(히어로 이미지, 상단 타일 6개 등)에는 반드시 `fetchpriority="high"`를 명시하여 브라우저가 가장 먼저 이미지를 다운받도록 합니다. 반면 화면 아래 보이지 않는 썸네일들은 `loading="lazy"`와 `fetchpriority="low"`를 부여하여 중요도를 낮춥니다.

## 5. Git Commit Guidelines 및 형상 관리(Version Control) 원칙
본 프로젝트의 모든 변경 사항은 추후 트러블슈팅과 히스토리 추적을 위해 완벽한 형태의 Git Commit History로 남아있어야 합니다. 단순한 "Update"나 "Fix bug" 같은 성의 없고 기계적인 AI식 커밋 메시지 작성은 절대로 허용되지 않으며, 변경의 **이유(Why)**와 **맥락(Context)**이 가장 상세하게 기록되어야 합니다. 아래의 엄격한 가이드라인을 단 하나라도 어겨서는 안 됩니다.

### 5-1. 커밋 메시지 언어 및 기본 형식 강제
- **작성 언어:** 모든 커밋 메시지의 제목(Subject)과 본문(Body)은 반드시 **한국어(한글)**로 작성해야 합니다. 영어를 섞어 쓰는 것은 기술 용어(예: `useEffect`, `Firestore`, `Ollama` 등)에 한정합니다.
- **포맷 강제 (Conventional Commits 변형):** 커밋 메시지는 반드시 `타입(Scope): 제목` 형태로 시작해야 하며, 본문과 한 줄의 빈 줄로 구분되어야 합니다.
  ```text
  타입(스코프): 커밋 제목 (50자 이내, 핵심만 요약)

  본문 (상세한 변경 이유와 컨텍스트, 72자마다 줄바꿈)
  - 무엇을 변경했는가? (What)
  - 왜 변경했는가? (Why)
  - 어떤 부작용(Side Effect)이 예상되는가?

  꼬리말 (이슈 번호, Breaking Change 등)
  ```

### 5-2. 커밋 타입 (Commit Type) 분류 규칙
어떤 작업을 했는지 명확히 알 수 있도록, 반드시 아래에 지정된 8가지 타입 중 하나로 시작해야 합니다.
- **`feat` (기능):** 새로운 기능이나 화면, API 라우트를 추가할 때 사용합니다. (예: `feat(crawler): 유튜브 커뮤니티 정규식 스크래핑 기능 추가`)
- **`fix` (버그 수정):** 기존 기능의 치명적인 오류나 버그, 환각(Hallucination) 현상을 해결했을 때 사용합니다. (예: `fix(parser): Qwen 환각으로 인해 인원수(20명)를 날짜로 착각하는 오탐 버그 해결`)
- **`docs` (문서화):** 리드미(`README.md`)나 현재 이 문서(`AGENTS.md`) 같은 정책/가이드 문서를 수정했을 때 사용합니다.
- **`style` (스타일링/UI):** 비즈니스 로직의 변경 없이 UI 컴포넌트의 CSS, 마진, 벤토 그리드 레이아웃, 컬러 테마 등을 변경했을 때 사용합니다. (예: `style(calendar): 벤토 타일 간격 12px 강제 및 모서리 곡률 조정`)
- **`refactor` (리팩터링):** 기능의 결과는 동일하지만 코드를 더 읽기 쉽고 성능이 좋게 재구성했을 때 사용합니다. (예: `refactor(db): Alias 크로스체크 루프를 Set 자료구조로 변경하여 O(1) 조회로 최적화`)
- **`perf` (성능):** LCP, INP, 메모리 누수 방지 등 렌더링 성능이나 실행 속도를 크게 개선한 경우 사용합니다.
- **`test` (테스트):** 테스트 코드(Jest, Cypress 등)를 추가하거나 수정했을 때 사용합니다.
- **`chore` (잡일):** 패키지 설치(`package.json` 변경), 빌드 스크립트 수정, 기타 설정 파일(`next.config.js`) 변경 등 애플리케이션 실제 동작과 무관한 변경 시 사용합니다.

### 5-3. 커밋 본문 (Body) 작성 철칙: "HOW"가 아닌 "WHY"에 집중할 것
- "코드를 어떻게(How) 고쳤는가"는 이미 Git Diff를 보면 알 수 있습니다. 절대 커밋 본문에 코드를 어떻게 짰는지 기계적으로 나열하지 마십시오.
- **반드시 명시해야 할 것:** 이 변경이 왜 필요했는지(Why), 기존 코드의 어떤 부분이 사용자에게 피해를 주었는지(Context), 그리고 이 패치로 인해 다른 컴포넌트에 어떤 영향을 미치는지(Side Effect)를 소설 쓰듯 가장 구체적으로 명시해야 합니다.
  - ❌ [나쁜 예]: `fix(crawler): 7일 조건문 추가. Qwen 프롬프트 수정함.`
  - ✅ [좋은 예]: 
    ```text
    fix(crawler): 과거 컴백 데이터 영구 누락 버그 해결 및 환각 방지책 추가

    [문제 원인]
    과거 스크립트에서 `releaseDate < todayStr` 비교 로직을 하드코딩하여, 크롤러 실행 시점 기준 하루라도 지난 앨범 발매 데이터(아이덴티티, 영파씨 등)가 통째로 스킵되는 끔찍한 데이터 유실 사태가 발생함.
    또한 LLM이 기사 본문의 '20명'이라는 인원수를 '20일'로 착각하는 치명적 환각 버그가 있었음.

    [해결책]
    1. todayStr 대신 과거 7일을 포함하는 롤링 윈도우(sevenDaysAgoStr) 로직으로 교체하여 어제/오늘 발매건이 정상 수집되도록 보장함.
    2. Qwen 프롬프트에 '숫자(N명, N주년)를 날짜로 착각하지 말 것'이라는 강력한 예외 처리 문구를 하드코딩함.
    ```

### 5-4. 브랜치(Branch) 네이밍 및 병합(Merge) 전략
- Main 브랜치(`main` 또는 `master`)에는 절대 직접 커밋(Direct Commit)하지 마십시오.
- 모든 작업은 반드시 `타입/이슈단어-요약` 형태의 브랜치에서 진행되어야 합니다.
  - 예: `feature/youtube-scraper`, `hotfix/alias-merge-error`, `refactor/bento-grid-ui`
- 작업이 완료되면 코드 리뷰(혹은 자체 검증)를 거친 후 Main 브랜치로 병합하며, 섣부른 병합으로 인해 기존 기능이 깨지는 일(Regression)이 없도록 테스트 스크립트를 먼저 구동해야 합니다.

### 5-5. 파괴적 변경 (Breaking Changes) 명시 의무
- 만약 DB 스키마 필드명을 바꾸거나, 크롤러의 핵심 파싱 로직을 통째로 엎어서 기존 데이터들과 심각한 호환성 문제가 생기는 작업일 경우, 커밋 메시지 맨 하단(꼬리말)에 반드시 `BREAKING CHANGE:` 키워드를 대문자로 명시하고 어떤 조치가 추가로 필요한지 경고해야 합니다.
  - 예: `BREAKING CHANGE: artists 컬렉션의 name 필드가 string에서 {ko, en} 객체로 변경됨. 프론트엔드 전체의 렌더링 에러를 막기 위해 마이그레이션 스크립트를 먼저 실행해야 함.`


> **관제 규칙:** 매번 Agent.md를 읽을 때마다 문서 맨 뒤에 있는 숫자를 1씩 증가시킬 것.

### Agent Read Count
7